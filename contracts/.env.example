# Used by most Hardhat networks (hex private key, no 0x prefix is ok if your tooling accepts it)
DEPLOYER_PRIVATE_KEY=YOUR_PRIVATE_KEY_HERE

# Receives TokenFactory service fees (defaults to deployer if unset)
# PLATFORM_WALLET=0x...
